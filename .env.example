# Environment Configuration

# OpenRouter API Key (FREE - No signup required!)
# Use the free tier key below, or get your own at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-free

# OpenRouter API endpoint
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# LLM Configuration - FREE MODELS
# Available free models:
# - google/gemma-2-9b-it:free (Google's Gemma 2, recommended)
# - meta-llama/llama-3.1-8b-instruct:free (Meta's Llama 3.1)
# - mistralai/mistral-7b-instruct:free (Mistral 7B)
LLM_MODEL=google/gemma-2-9b-it:free

# Embedding Model - FREE
# Available free embedding models:
# - thenlper/gte-large:free (1024 dims, recommended)
# - nomic-ai/nomic-embed-text-v1.5:free (768 dims)
EMBEDDING_MODEL=thenlper/gte-large:free
EMBEDDING_DIMENSION=1024

# Vector Database
CHROMA_PERSIST_DIRECTORY=./chroma_db
CHROMA_COLLECTION_NAME=documents

# Chunking Configuration
CHUNK_SIZE=1000
CHUNK_OVERLAP=200

# RAG Configuration
TOP_K=5
TEMPERATURE=0.7
MAX_TOKENS=1000

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
CORS_ORIGINS=["http://localhost:3000", "http://localhost:5173"]
